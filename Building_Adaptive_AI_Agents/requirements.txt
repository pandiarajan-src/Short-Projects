networkx>=3.0
scikit-learn>=1.3
matplotlib>=3.7
numpy>=1.24

# Optional, only needed if you want a REAL LLM to write skills
# (otherwise the built-in heuristic writer is used automatically)
openai>=1.0          # works with Ollama's OpenAI-compatible endpoint too
